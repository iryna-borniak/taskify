<div>
  <h1 class="font-bold text-2xl mb-3">Editing Task</h1>

  <turbo-frame id="<%= dom_id @task %>" class="block">
    <%= render "form", task: @task %>
    <%= link_to "Never Mind", tasks_path,
                              class: "btn mb-3 bg-gray-100 mr-auto" %>
  </turbo-frame>
</div>
